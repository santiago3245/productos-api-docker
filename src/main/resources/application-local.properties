# Configuración para desarrollo local sin Docker

# Configuración del servidor
spring.application.name=productos-api
server.port=8080

# Configuración de SQL Server (local)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=ProductosDB;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=YourStrong@Passw0rd
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Logging
logging.level.root=INFO
logging.level.com.example.productosapi=DEBUG
